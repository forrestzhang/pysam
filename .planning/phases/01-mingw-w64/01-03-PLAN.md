---
phase: 01-mingw-w64
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - devtools/probe_symbol_check.py
  - setup.py
autonomous: true
requirements:
  - BUILD-01
  - BUILD-03

user_setup:
  - service: MSYS2 UCRT64
    why: "Build host for the Phase 1 dev build — D-15 fixes the split: MSYS2 itself is installed manually (one GUI run), the committed bootstrap script owns everything after"
    env_vars: []
    dashboard_config: []
    manual_steps:
      - "Install MSYS2 from https://www.msys2.org/ (default root C:\\msys64; record the root if different) and complete the first-update flow in the UCRT64 shell before executing this plan"

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One canonical command — pip install -e . --no-build-isolation inside a UCRT64 venv, delivered end-to-end by the committed bootstrap script — compiles all 13 Cython extensions plus the bundled htslib/htscodecs/samtools/bcftools, with no manual sh configure, make, or nm step (BUILD-01, ROADMAP success criterion 1)"
    - "import pysam succeeds in the UCRT64 venv, a generated BAM opens and yields a positive alignment count, and both a samtools and a bcftools command dispatch through _pysam_dispatch (ROADMAP success criterion 2, D-04)"
    - "The bootstrap run log — captured unconditionally on every execution path of Task 1 — contains no silent-skip warning, and the probe re-running setup.py's real llvm-nm parser (with its resolved _nm_command) against the built binaries proves the check executed with the ported tool (BUILD-03, ROADMAP success criterion 3)"
    - "The duplicate-detection teeth are proven with real built binaries: the check's own llvm-nm parser flags defined-symbol duplicates when one binary is judged as two extensions (BUILD-03 backstop)"
    - "ubuntu and macos jobs are green on the phase's final commit (ROADMAP success criterion 4, D-12; FreeBSD/NetBSD advisory only)"
  artifacts:
    - path: "pysam/ (13 libc*.pyd extension modules — untracked build outputs)"
      provides: "The compiled UCRT64 dev build: libchtslib, libcsamtools, libcbcftools, libcutils, and the nine feature modules"
      contains: "libchtslib"
    - path: "pysam/libchtslib.*.dll.a (untracked build output)"
      provides: "The libchtslib import library that resolves the 12 downstream -l flags (A3 checkpoint outcome)"
      contains: ".dll.a"
    - path: "devtools/probe_symbol_check.py"
      provides: "BUILD-03 teeth probe reusing setup.py's real llvm-nm parser against the built binaries"
      contains: "run_nm_defined_symbols"
  key_links:
    - from: "devtools/msys2-bootstrap.sh"
      to: "the venv import of pysam"
      via: "pacman packages -> _venv -> pip install -e . --no-build-isolation -> import pysam in that venv (D-07/D-14)"
      pattern: "_venv"
    - from: "pysam/libchtslib*.pyd"
      to: "libcsamtools/libcbcftools/libcutils and the nine feature modules"
      via: "import library pysam/libchtslib.*.dll.a at build time; LOAD_LIBRARY_SEARCH_DLL_LOAD_DIR sibling resolution at import time (A4 checkpoint outcome)"
      pattern: "chtslib"
    - from: "devtools/smoke_test.py"
      to: "_pysam_dispatch"
      via: "pysam.samtools.view and pysam.bcftools.view exercise the C dispatch path including the null-device plumbing fixed in plan 02"
      pattern: "samtools|bcftools"
  prohibitions:
    - statement: "MUST NOT package or distribute MinGW/UCRT64 build artifacts — no wheel or sdist target is built or published from the UCRT64 path in v1 (MinGW output is dev-validation only; wheels are MSVC-only in Phases 3-4; MinGW distribution is v2 scope)"
      status: unverified
      flagged: true

---

<objective>
Prove the phase end-to-end on real hardware: provision the UCRT64 environment with the committed bootstrap script, run the first full `pip install -e .` build, resolve the research's first-build checkpoints, execute the smoke gate, evidence BUILD-03 (check ran, no skip, teeth work), and close the phase with ubuntu+macos CI green on the final commit.

Purpose: ROADMAP success criteria 1-4 are all empirical — they can only be satisfied by building and running on the Windows UCRT64 toolchain. Every uncertainty left open by research (implib naming, llvm-nm on PE, macro necessity, configure flag set) resolves here.

Output: a working dev build, committed probe script, first-build findings in the SUMMARY, and a green fork CI run on the phase-final SHA.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-mingw-w64/01-CONTEXT.md
@.planning/phases/01-mingw-w64/01-RESEARCH.md
@.planning/phases/01-mingw-w64/01-PATTERNS.md
</context>

## Flagged assumptions (spec-less edge-probe rows — explicit, never silently dropped)

The phase has no SPEC Edge Coverage section; the fallback probe classified both requirement texts as `unclassified` -> `unresolved` (never auto-resolved, per the probe protocol). They are carried as explicit assumptions, not dropped:
- **BUILD-01 (unresolved):** acceptance rests on ROADMAP success criteria 1-2, lifted verbatim into `must_haves.truths` above and verified by Task 1/Task 2. The deterministic classifier surfaced no additional edge predicates for this build-pipeline requirement.
- **BUILD-03 (unresolved):** acceptance rests on ROADMAP success criterion 3, lifted into `must_haves.truths` and verified by Task 2. The teeth probe (Task 2) is the human-designed backstop for the duplicate-detection half that a build log cannot show.

First-build assumptions from research (A1 macros, A2 llvm-nm-on-PE, A3 implib naming, A4 sibling DLL resolution) each carry a designated checkpoint in Task 1/Task 2; an unconfirmed assumption there is surfaced in the SUMMARY, never assumed silently.

## Artifacts this phase produces

- `devtools/msys2-bootstrap.sh` — new committed UCRT64 provisioning script (D-13/D-15) — created by plan 01
- `devtools/smoke_test.py` — new committed acceptance smoke gate (D-04; reused as the Phase 4 CI smoke gate) — created by plan 01
- `devtools/probe_symbol_check.py` — new committed BUILD-03 teeth probe — created by this plan
- `.gitattributes` — new eol=lf rules for htslib/**, samtools/**, bcftools/**, *.sh, *.pyx — created by plan 01
- `INSTALL` — new "Windows (MSYS2 UCRT64)" section — created by plan 01
- `setup.py` — new module-level function `_is_ucrt64_python()`; new module-level function `_nm_command()`; the dead win32 branch rewritten in place (D-09); `define_macros` entries `('random', 'rand')` and `('srandom', 'srand')`; `-Wl,--out-implib,pysam/lib<EXT_SUFFIX-stem>.dll.a` link flag on pysam.libchtslib; `sh ` prefix inside `run_configure` on win32; win32 elif in `cy_build_ext.build_extension`; restructured symbol-check dispatch in `cy_build_ext.run` — created by plan 02, possibly amended here by first-build fixes
- `pysam/libcutils.pyx` — two `c_open(force_bytes(os.devnull), O_WRONLY)` call sites (lines 383 and 390) — created by plan 02
- Deleted: `win32/unistd.h`, `win32/stdint.h` (D-10) — by plan 02
- Built artifacts (never committed): `htslib/config.h`, `htslib/libhts.a`, `pysam/config.py`, 13 `pysam/libc*.pyd` extension modules, `pysam/libchtslib.*.dll.a` import library

<tasks>

<task type="auto">
  <name>Task 1: Provision UCRT64 via the bootstrap script and land the first full build</name>
  <precondition>MSYS2 UCRT64 is installed (user_setup complete): the UCRT64 shell exists — default root /c/msys64 with bash at /c/msys64/usr/bin/bash.exe, and its first-run update flow was completed. If installed at a different root, substitute that root in every path below and record it in the SUMMARY.</precondition>
  <files>setup.py</files>
  <read_first>
    - devtools/msys2-bootstrap.sh (the deliverable being executed; its troubleshooting section is the clean-state recipe)
    - .planning/phases/01-mingw-w64/01-RESEARCH.md (sections: Open Questions 1-3, Assumptions Log A1-A4, Common Pitfalls 2-12 — each names the warning sign to watch for in the first build)
    - setup.py (the plan 02 state being exercised)
  </read_first>
  <action>
    All commands run from the repo checkout via the UCRT64 environment (D-06: the checkout stays at /d/Github/pysam; builds are slower on drvfs — use generous command timeouts, the full build is many minutes).
    1. First attempt, straight through the deliverable: `MSYSTEM=UCRT64 /c/msys64/usr/bin/bash -lc 'cd /d/Github/pysam && sh devtools/msys2-bootstrap.sh 2>&1 | tee /tmp/pysam-bootstrap-run.log'`. If it completes, skip to step 4.
    2. On failure, debug stage-by-stage in the same UCRT64 bash (the script is the spec, the stages are its lines): pacman packages (idempotent --needed), venv creation, then the build alone with a verbose captured log: `python -m pip install -v -e . --no-build-isolation 2>&1 | tee /tmp/pysam-ucrt64-build.log`. Fix forward only in D-08 style — small win32-gated setup.py amendments committed with `build(01): <what the first build taught>` — and re-run. Known checkpoints to resolve as they fire (A1: whether the vcfsom macros are required or harmlessly redundant; A2: llvm-nm parses the built PE binaries; A3: the implib file `ls pysam/libchtslib.*.dll.a` exists with the expected lib-prefixed stub name; OQ1: `sh configure` completes under --disable-ref-cache plus --disable-libcurl and htslib/config.h is real — the empty-config fallback path at setup.py:535-541 must NOT be hit; Pitfall 8: dispatch failures only at command stage, not import/BAM-open, would mean the null-device fix missed a site).
       Budget tripwire (hard bound on this loop): cap the debug-fix-rebuild cycle at two full iterations per stage. Before starting the clean-slate re-run in step 3, both gates must hold — no stage has consumed more than two failed rebuild attempts, and you judge at least a third of the plan's context budget still available. If either gate fails, stop in a controlled way instead of looping on: commit any D-08-style fixes already made, record in the SUMMARY which of A1/A2/A3/A4/OQ1 resolved, the failing stage, its captured log path, and the single next command to resume — then surface a blocking `checkpoint:human-verify` carrying that resume payload. The clean-slate run in step 3 is itself capped at one attempt before the same controlled stop applies.
    3. Once the build succeeds, prove the canonical flow from a clean slate (Pitfall 11): stop, clean per the bootstrap script's troubleshooting section (`git clean -xfd htslib samtools bcftools`, delete `pysam/config.py`, `pysam/*.pyd`, `pysam/*.dll.a`, any `pysam.egg-info`, and optionally `_venv`), and run the bootstrap script end-to-end again from scratch with the same capture as step 1: `MSYSTEM=UCRT64 /c/msys64/usr/bin/bash -lc 'cd /d/Github/pysam && sh devtools/msys2-bootstrap.sh 2>&1 | tee /tmp/pysam-bootstrap-run.log'` — overwriting the log so its final content is the clean-state acceptance run (this is also the log every downstream no-skip grep reads). This clean-state run is the acceptance proof that the committed deliverable works for a fresh developer (D-13/D-14).
    4. Verify the built state: inside the UCRT64 venv, `python -c "import pysam; print(pysam.__version__, pysam.config.HTSLIB)"` must print a version and `builtin` (proves the config.py generation from the MSYS2-produced htslib/config.h worked), and `ls pysam/*.pyd | wc -l` must report at least 13.
    5. Record in the SUMMARY: which of A1/A2/A3/A4/OQ1 resolved which way, the exact implib filename produced, and any setup.py amendments committed.
  </action>
  <verify>
    <automated>MSYSTEM=UCRT64 /c/msys64/usr/bin/bash -lc 'cd /d/Github/pysam && . _venv/bin/activate && python -c "import pysam; print(pysam.__version__, pysam.config.HTSLIB)"'</automated>
    <fails_when>non-zero exit, or the printed line does not contain `builtin` (import failed or config generation produced the wrong source marker)</fails_when>
    <automated>MSYSTEM=UCRT64 /c/msys64/usr/bin/bash -lc 'cd /d/Github/pysam && ls pysam/*.pyd | wc -l'</automated>
    <fails_when>printed count is less than 13</fails_when>
    <automated>MSYSTEM=UCRT64 /c/msys64/usr/bin/bash -lc 'cd /d/Github/pysam && ls pysam/libchtslib.*.dll.a'</automated>
    <fails_when>non-zero exit (the import library A3 checkpoint did not produce its artifact)</fails_when>
    <automated>grep -c "smoke OK" /tmp/pysam-bootstrap-run.log</automated>
    <fails_when>output is `0` (the clean-state end-to-end bootstrap run, which ends with the smoke gate, never completed)</fails_when>
  </verify>
  <acceptance_criteria>
    - The committed bootstrap script runs end-to-end from a clean slate: pacman idempotent, venv created, build completes, smoke gate passes, `smoke OK` in the captured log
    - At least 13 .pyd files exist under pysam/ and pysam/libchtslib.*.dll.a exists with the EXT_SUFFIX-derived stub name
    - import pysam in the UCRT64 venv prints a version and HTSLIB=builtin
    - htslib/config.h is a real configure product (the empty-fallback path was never hit)
    - All first-build checkpoint outcomes (A1, A2, A3, A4, OQ1) are recorded in the SUMMARY; any setup.py amendments are committed in D-08 style
    - No wheel, sdist, or distribution artifact was built (MinGW output is dev-validation only)
  </acceptance_criteria>
  <done>The one-command build (D-14) works on real hardware and every research assumption that could only be tested by building has a recorded outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Run the smoke gate and prove BUILD-03 ran, did not skip, and has teeth</name>
  <precondition>The UCRT64 venv build from Task 1 exists: `_venv` activates in the UCRT64 shell and import pysam succeeds there.</precondition>
  <files>devtools/probe_symbol_check.py</files>
  <read_first>
    - setup.py lines 94-112 (run_nm_defined_symbols — the function the probe must reuse verbatim, including its nested cython_internal helper and the keep-filter at line 104) and lines 320-337 (check_ext_symbol_conflicts — the duplicate rule the probe re-states so it can feed synthetic inputs)
    - devtools/smoke_test.py (the gate being executed)
    - .planning/phases/01-mingw-w64/01-RESEARCH.md (Pitfall 3 — the exact skip-warning behavior that must be absent on Windows; Assumption A2 — llvm-nm on PE/COFF is the checkpoint this task settles)
  </read_first>
  <action>
    1. Smoke gate (D-04): inside the UCRT64 venv, `python devtools/smoke_test.py` must print `smoke OK` and exit 0. A failure isolated to command dispatch (import and BAM-open fine) points at the null-device plumbing — fix forward, commit, re-run.
    2. No-skip evidence (BUILD-03): grep `/tmp/pysam-bootstrap-run.log` for the POSIX silent-skip warning phrase emitted at setup.py lines 365/367 and require zero hits on the Windows build. That log — tee'd unconditionally by Task 1 step 1 and overwritten by the clean-state run in its step 3 — captures the build's stdout/stderr on every execution path, including any setup.py `log.warning` emitted during `pip install`. Never source this grep from `/tmp/pysam-ucrt64-build.log`: that file exists only on Task 1's debug path. Do not look for the tool name in the log either — llvm-nm's output is captured by setup.py's subprocess, so the string is not reliably echoed on the success path; the proof that the check actually executed with the ported tool comes from step 3's direct llvm-nm invocation and step 4's probe assertion on the real `_nm_command()` resolution. The POSIX warning string appears in setup.py only inside the POSIX-gated branch — verify that region-scope with `git diff master -- setup.py` if the grep surprises you.
    3. Tool-on-PE evidence (A2): run `MSYSTEM=UCRT64 /c/msys64/usr/bin/bash -lc 'cd /d/Github/pysam && llvm-nm -g -P pysam/libcsamtools*.pyd | head -20'` and confirm the output is non-empty POSIX.2 lines (symbol name, then a type letter) — llvm-nm reads the built binaries.
    4. Teeth probe (BUILD-03 backstop): create committed `devtools/probe_symbol_check.py`:
       - Parse setup.py with ast and extract the source of `_nm_command` and `run_nm_defined_symbols` (with its nested helper); exec both into one namespace providing `subprocess` and `IS_DARWIN = False` — the extracted body calls `_nm_command()`, so both must come from the same extraction. Do not re-implement or copy the parser — the probe must exercise the real llvm-nm invocation and parsing code against the built binaries.
       - Tool evidence: print `_nm_command()` and assert it equals the llvm-nm triple (`llvm-nm -g -P`) in the UCRT64 venv — the real build-time tool resolution, proven without depending on log echo behavior; any other value fails the probe.
       - Enumerate the built modules via `glob.glob(os.path.join("pysam", "libc*.pyd"))`; expect at least 13.
       - Apply the check's duplicate rule (re-stated in the probe, 8 lines, mirroring setup.py:326-337 so synthetic inputs can be fed): for each pseudo-extension, collect defined symbols via the extracted parser (keep symbols whose type letter is not in U F N W w and are not cython-internal, minus the non-darwin leading-punctuation filter), then flag any symbol claimed by more than one pseudo-extension.
       - Teeth assertion: judge the SAME binary under two distinct pseudo-names — the duplicate count must exceed zero (the check would raise; this proves duplicates are detected, not silently absorbed).
       - Honesty assertion: judge the 13+ real distinct binaries — the duplicate count must be zero, independently re-validating the build-time check result.
       - Print both counts; exit 0 only when both assertions hold, nonzero otherwise. If the teeth assertion yields zero duplicates, llvm-nm output was empty or unparseable — that is a failed A2 checkpoint, not a pass.
       - Commit with message `build(01): add symbol-conflict teeth probe`.
    5. Record in the SUMMARY: the smoke step timings if notable, the A2 outcome, and the two probe counts.
  </action>
  <verify>
    <automated>MSYSTEM=UCRT64 /c/msys64/usr/bin/bash -lc 'cd /d/Github/pysam && . _venv/bin/activate && python devtools/smoke_test.py'</automated>
    <fails_when>non-zero exit, or `smoke OK` absent from output</fails_when>
    <automated>grep -c "skipping symbol collision check" /tmp/pysam-bootstrap-run.log</automated>
    <fails_when>output is greater than `0` (the Windows build took a silent-skip path — BUILD-03 violation)</fails_when>
    <automated>MSYSTEM=UCRT64 /c/msys64/usr/bin/bash -lc 'cd /d/Github/pysam && llvm-nm -g -P pysam/libcsamtools*.pyd | head -20'</automated>
    <fails_when>empty output (llvm-nm produced nothing for a built PE binary — A2 checkpoint failed)</fails_when>
    <automated>MSYSTEM=UCRT64 /c/msys64/usr/bin/bash -lc 'cd /d/Github/pysam && . _venv/bin/activate && python devtools/probe_symbol_check.py'</automated>
    <fails_when>non-zero exit, or the printed teeth count is 0 (duplicates not detected by the real parser), or the printed resolved nm command is not the llvm-nm triple (the check's real code did not select the ported tool)</fails_when>
  </verify>
  <acceptance_criteria>
    - devtools/smoke_test.py exits 0 printing `smoke OK` in the UCRT64 venv: BAM generated via samtools dispatch, magic bytes verified, alignments counted, bcftools dispatch returned data
    - /tmp/pysam-bootstrap-run.log (captured on every execution path) contains zero occurrences of the silent-skip warning; llvm-nm execution is proven by the direct llvm-nm invocation and the probe's resolved-command assertion, not by a log string
    - llvm-nm -g -P emits non-empty POSIX.2 lines for a built .pyd (A2 resolved positively, recorded in SUMMARY)
    - devtools/probe_symbol_check.py is committed and exits 0 with a teeth count above zero and a real-binaries count of zero
    - No wheel or sdist artifact was produced at any point
  </acceptance_criteria>
  <done>Success criteria 2 and 3 are empirically proven: the built pysam works from Python, and the symbol gate demonstrably ran, did not skip, and detects duplicates.</done>
</task>

<task type="auto">
  <name>Task 3: Push the phase-final commit and close on green fork CI</name>
  <precondition>git push access to the fork origin (git@github.com:forrestzhang/pysam.git) — `git ls-remote --heads origin win` succeeding proves it.</precondition>
  <files>setup.py</files>
  <read_first>
    - .github/workflows/ci.yaml (the ubuntu/macos matrix and its push trigger — the D-11/D-12 proof mechanism; FreeBSD/NetBSD VM jobs are advisory per D-12)
    - .planning/phases/01-mingw-w64/01-CONTEXT.md (decisions D-11, D-12)
  </read_first>
  <action>
    1. Confirm the tree is fully committed: `git status --porcelain` must be empty (build artifacts are untracked by design — never commit .pyd, config.h, libhts.a, config.py, egg-info, or _venv). Any remaining plan edits get committed first with their own descriptive message.
    2. Push the phase-final head (D-11): `git push origin win`; on a non-fast-forward rejection, `git pull --rebase origin win` then push again. Record the pushed SHA.
    3. Watch CI on that exact SHA (D-12): `gh run list --branch win --limit 1 --json databaseId,headSha` — confirm headSha equals the pushed SHA, then `gh run watch <databaseId> --exit-status`. Judge only the ubuntu and macos matrix jobs as blocking; evaluate their conclusions explicitly via `gh run view <databaseId> --json jobs`. If gh is not authenticated, surface a `checkpoint:human-action` auth gate (`gh auth login`) and resume after — never skip or approximate CI verification.
    4. On any ubuntu/macos failure: reproduce the failing job's log (`gh run view <databaseId> --log-failed`), fix the regression in D-08 style, commit, re-push, re-watch — repeat until green. The phase closes on the green final SHA, not on a best effort.
    5. Update `.planning/ROADMAP.md` progress table row for Phase 1 as part of wrap-up only if the orchestrator has not already done so.
  </action>
  <verify>
    <automated>git status --porcelain</automated>
    <fails_when>non-empty output (uncommitted phase work left behind)</fails_when>
    <automated>git rev-parse HEAD; git rev-parse origin/win</automated>
    <fails_when>the two SHAs differ (the local phase-final commit is not what the fork CI ran on)</fails_when>
    <automated>gh run view "$(gh run list --branch win --limit 1 --json databaseId --jq '.[0].databaseId')" --json jobs --jq '[.jobs[] | select(.name | test("ubuntu|macos";"i")) | .conclusion] | length, any(. != "success")'</automated>
    <fails_when>the job count is 0, or the any() field prints true (a blocking ubuntu/macos job did not succeed on the final SHA)</fails_when>
  </verify>
  <acceptance_criteria>
    - The working tree is clean; origin/win points at the local phase-final commit
    - The fork CI run for that SHA shows every ubuntu job and every macos job with conclusion success (D-12); BSD VM results are ignored
    - Any CI failure encountered was fixed, committed, re-pushed, and re-verified to green
  </acceptance_criteria>
  <done>Success criterion 4 closed: ubuntu and macos builds are green on the phase's final commit, proving zero POSIX regression across every wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pacman repos -> dev machine | toolchain/library packages install during provisioning |
| UCRT64 build outputs -> Python runtime | built .pyd binaries load into the interpreter (sibling DLL resolution) |
| built artifacts -> distribution surface | the point where a dev-validation build could wrongly become a distributable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | pacman package installs during bootstrap | high | mitigate | GPG-signed MSYS2 repos only; --no-build-isolation keeps PyPI binary wheels off the build path; all nine packages carry Approved verdicts in the 01-RESEARCH.md Package Legitimacy Audit (zero [ASSUMED]/[SUS]), so no blocking legitimacy checkpoint is required |
| T-01-09 | Tampering | built .pyd loading at import time | medium | mitigate | Sibling resolution relies on the standard CPython DLL search of the loaded extension's own directory (A4); the smoke gate empirically proves the loaded set is the just-built one; no PATH-based DLL dependency is introduced |
| T-01-10 | Information Disclosure / Tampering | accidental distribution of dev-validation artifacts | medium | mitigate | Prohibition above: no wheel/sdist target is invoked in this plan; verify commands assert only import/smoke/gate outcomes, never packaging; acceptance_criteria forbid committing or building distribution artifacts |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | None occur (pacman GPG-signed packages only; venv uses pacman setuptools/Cython via --no-build-isolation); Package Legitimacy Audit table present in 01-RESEARCH.md with zero flagged verdicts, so no blocking human checkpoint is inserted |
</threat_model>

<verification>
- Clean-state `sh devtools/msys2-bootstrap.sh` run completes end-to-end with `smoke OK` in its log (success criterion 1)
- `import pysam` + config.HTSLIB == builtin + at least 13 .pyd + libchtslib.*.dll.a present
- `python devtools/smoke_test.py` exits 0 printing `smoke OK` (success criterion 2, D-04)
- Bootstrap run log (/tmp/pysam-bootstrap-run.log, captured on every path): zero silent-skip warnings; llvm-nm executed, proven by the direct llvm-nm invocation and the probe's resolved-command assertion; probe reports teeth > 0 and real-binaries duplicates == 0 (success criterion 3)
- origin/win == local HEAD; ubuntu and macos jobs green on that SHA (success criterion 4, D-12)
- First-build checkpoint outcomes (A1-A4, OQ1) recorded in the SUMMARY
</verification>

<success_criteria>
- ROADMAP success criteria 1-4 all hold on empirical evidence captured in this plan's verify commands and logs
- The committed bootstrap script reproduces the build from a clean slate for the next developer
- BUILD-03 is proven, not just implemented: the gate ran on Windows, cannot silently skip there, and demonstrably detects duplicate defined symbols
- The phase closes with the fork's POSIX CI green on the final SHA — zero-regression constraint upheld
</success_criteria>

<output>
Create `.planning/phases/01-mingw-w64/01-03-SUMMARY.md` when done
</output>
