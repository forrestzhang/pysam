---
phase: 01-mingw-w64
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - devtools/msys2-bootstrap.sh
  - devtools/smoke_test.py
  - INSTALL
autonomous: true
requirements:
  - BUILD-01

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "git operations can no longer produce CRLF-mangled bundled shell scripts or Makefiles: htslib/configure, htslib/Makefile, htslib/htscodecs.mk, bcftools/vcfsom.c report w/lf after renormalization (Pitfall 1 blocker cleared)"
    - "A developer who has installed MSYS2 can provision the complete UCRT64 build environment (9 pacman packages, env sanity, llvm-nm presence, venv, editable build, smoke gate) by running one committed script"
    - "The committed smoke script exits nonzero on any failure, uses binary mode on every direct open call, asserts BAM magic bytes, and exercises both a samtools and a bcftools dispatch"
    - "INSTALL documents exactly two manual steps (install MSYS2 from the official URL, open the UCRT64 shell) plus the scripted bootstrap, with the canonical one-command build (D-14) and the GCC + python.org unsupported sentence (D-05)"
  artifacts:
    - path: ".gitattributes"
      provides: "eol=lf rules for htslib/**, samtools/**, bcftools/**, *.sh, *.pyx (keeps existing export-ignore/linguist rules)"
      contains: "eol=lf"
    - path: "devtools/msys2-bootstrap.sh"
      provides: "UCRT64 provisioning script mirroring devtools/install-prerequisites.sh structure; PKGS list is the D-03/D-13 single source of truth"
      contains: "mingw-w64-ucrt-x86_64-llvm-tools"
    - path: "devtools/smoke_test.py"
      provides: "Phase acceptance smoke gate (D-04), designed for reuse as the Phase 4 CI smoke gate"
      contains: "pysam_data"
    - path: "INSTALL"
      provides: "Windows (MSYS2 UCRT64) section after the existing install sections"
      contains: "UCRT64"
  key_links:
    - from: "devtools/msys2-bootstrap.sh"
      to: "setup.py (plan 02 fail-fast gate message)"
      via: "identical 9-package pacman list verbatim (D-03 + D-13 single source of truth)"
      pattern: "mingw-w64-ucrt-x86_64-(python|python-pip|python-setuptools|python-cython|toolchain|llvm-tools|zlib|bzip2|xz)"
    - from: "devtools/msys2-bootstrap.sh"
      to: "devtools/smoke_test.py"
      via: "bootstrap's final step invokes the smoke gate"
      pattern: "devtools/smoke_test\\.py"
    - from: "devtools/smoke_test.py"
      to: "tests/pysam_data/ex1.sam.gz and tests/pysam_data/ex1.vcf.gz"
      via: "committed test data inputs resolved relative to the repo root"
      pattern: "pysam_data"

---

<objective>
Lay the Phase 1 build foundations: repair the CRLF-mangled working tree, commit the UCRT64 bootstrap script (D-13/D-15), the acceptance smoke gate (D-04), and the INSTALL Windows section (D-14/D-15/D-05).

Purpose: The bundled htslib/samtools/bcftools shell scripts and Makefiles are CRLF-mangled on disk right now (core.autocrlf=true), which makes `sh configure` and `make` impossible — this is a Wave-0 blocker (Pitfall 1). The bootstrap script and smoke gate are the provisioning and acceptance contracts that plans 02/03 build and verify against.

Output: `.gitattributes` eol rules + renormalized working tree; `devtools/msys2-bootstrap.sh`; `devtools/smoke_test.py`; INSTALL Windows section — all committed.

Tracer note: this plan's first task is the phase's tracer slice — the thinnest repo-to-toolchain path (checkout hygiene layer -> provisioning contract layer) with runnable verifications. The live end-to-end run of this path (pacman install -> venv -> build) executes in plan 03, gated on the manual MSYS2 install (D-15); wave 1 stays fully autonomous by design.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mingw-w64/01-CONTEXT.md
@.planning/phases/01-mingw-w64/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `devtools/msys2-bootstrap.sh` — new committed UCRT64 provisioning script (D-13/D-15)
- `devtools/smoke_test.py` — new committed acceptance smoke gate (D-04; reused as the Phase 4 CI smoke gate)
- `devtools/probe_symbol_check.py` — new committed BUILD-03 teeth probe (created by plan 03)
- `.gitattributes` — new eol=lf rules for htslib/**, samtools/**, bcftools/**, *.sh, *.pyx
- `INSTALL` — new "Windows (MSYS2 UCRT64)" section
- `setup.py` — new module-level function `_is_ucrt64_python()`; new module-level function `_nm_command()`; the dead win32 branch rewritten in place (D-09); `define_macros` entries `('random', 'rand')` and `('srandom', 'srand')`; `-Wl,--out-implib,pysam/lib<EXT_SUFFIX-stem>.dll.a` link flag on pysam.libchtslib; `sh ` prefix inside `run_configure` on win32; win32 elif in `cy_build_ext.build_extension`; restructured symbol-check dispatch in `cy_build_ext.run`
- `pysam/libcutils.pyx` — two `c_open(force_bytes(os.devnull), O_WRONLY)` call sites (lines 383 and 390)
- Deleted: `win32/unistd.h`, `win32/stdint.h` (D-10)
- Built artifacts (never committed): `htslib/config.h`, `htslib/libhts.a`, `pysam/config.py`, 13 `pysam/libc*.pyd` extension modules, `pysam/libchtslib.*.dll.a` import library

<tasks>

<task type="tracer">
  <name>Task 1: Renormalize the working tree to LF and commit the UCRT64 bootstrap script</name>
  <reversibility rating="costly">Implements D-01 (MSYS2 pacman python as the Phase 1 target): the bootstrap package list, the fail-fast message contract, and the venv flow all encode this choice; Phases 2-4 build on it, so undoing it later is a coordinated change across script, docs, and setup.py.</reversibility>
  <files>.gitattributes, devtools/msys2-bootstrap.sh</files>
  <read_first>
    - .gitattributes (current content: only export-ignore and linguist-vendored rules, no eol rules)
    - devtools/install-prerequisites.sh (structural analog to mirror per D-13: same shebang style `#!/bin/sh -e`, detection gate first, native package-manager install, post-install verification)
    - .planning/phases/01-mingw-w64/01-RESEARCH.md (sections: Standard Stack installation block — the authoritative 9-package list; Common Pitfalls 1, 10, 11; Runtime State Inventory — HTSLIB_MODE env hazard and the documented clean step; Code Examples — bootstrap script skeleton)
  </read_first>
  <action>
    1. Append eol rules to `.gitattributes` (keep all existing lines): `htslib/** text eol=lf`, `samtools/** text eol=lf`, `bcftools/** text eol=lf`, `*.sh text eol=lf`, `*.pyx text eol=lf`. Commit `.gitattributes` alone first, message `build(01): force LF eol for bundled sources and scripts`.
    2. Renormalize the CRLF-mangled working tree (machine has core.autocrlf=true; htslib/configure carries CR bytes right now): delete the working-tree copies of tracked files under the covered paths (`git ls-files -z htslib samtools bcftools devtools | xargs -0 rm -f --`), then restore them from the index with the new filters (`git checkout -- htslib samtools bcftools devtools`). Git-detected binaries under those trees are untouched because the `text` attribute is bare (auto). `git status` must stay clean afterward — renormalization changes no index content.
    3. Create `devtools/msys2-bootstrap.sh` mirroring `devtools/install-prerequisites.sh` structure (shebang `#!/bin/sh -e`, no set -euo pipefail ceremony):
       - Header comment: manual step 0 per D-15 — install MSYS2 from https://www.msys2.org/ and open the UCRT64 shell once; everything after is owned by this script.
       - Define `PKGS` as a single-quoted, single-line, space-joined assignment containing exactly these nine full package names in this order (D-02: no versions pinned): mingw-w64-ucrt-x86_64-python mingw-w64-ucrt-x86_64-python-pip mingw-w64-ucrt-x86_64-python-setuptools mingw-w64-ucrt-x86_64-python-cython mingw-w64-ucrt-x86_64-toolchain mingw-w64-ucrt-x86_64-llvm-tools mingw-w64-ucrt-x86_64-zlib mingw-w64-ucrt-x86_64-bzip2 mingw-w64-ucrt-x86_64-xz
       - UCRT64 gate first (analog of the POSIX script's detection chain): if MSYSTEM is not UCRT64, print an error telling the user to run inside the UCRT64 shell and exit 1.
       - Env sanity per the Runtime State Inventory hazard: if HTSLIB_MODE is set to anything other than the empty string or shared, error and exit.
       - `pacman -Syu --needed $PKGS`, with a comment documenting two known behaviors: pacman may offer to replace binutils tools with llvm-tools (accept — Pitfall 10), and the first-ever `pacman -Syu` may ask to terminate the shell (reopen UCRT64 and rerun; the script is idempotent via --needed).
       - Post-install verification: loop over $PKGS printing `pacman -Qi` one-line version info to the log (D-02), then `command -v llvm-nm` check (fail with a message naming the llvm-tools package) and `llvm-nm --version`.
       - Export HTSLIB_CONFIGURE_OPTIONS to --disable-libcurl before the build (D-context pre-locked constraint: libcurl/remote I/O stays off on Windows in v1; REMOTE-01 deferred to v2).
       - `python -m venv --system-site-packages _venv` then `. _venv/bin/activate` (D-07: dev install into a venv, not pacman site-packages).
       - `python -m pip install -e . --no-build-isolation` (D-14 canonical command; pacman setuptools/Cython incompatible with PyPI binary wheels, so isolation stays off).
       - Final step: `python devtools/smoke_test.py` (D-04 acceptance gate; created by Task 2 of this plan).
       - Troubleshooting section in comments (Pitfall 11): the documented clean step before any rebuild after a config change — `git clean -xfd htslib samtools bcftools`, delete `pysam/config.py`, `pysam/*.pyd`, `pysam/*.dll.a`, any `pysam.egg-info`, and optionally `_venv` — plus the stale-libhts.a warning (prebuild_libchtslib skips when htslib/libhts.a exists).
    4. Commit `devtools/msys2-bootstrap.sh` with message `build(01): add UCRT64 bootstrap script`.
  </action>
  <verify>
    <automated>git check-attr eol -- htslib/configure htslib/Makefile htslib/htscodecs.mk samtools/samtools.pysam.c bcftools/vcfsom.c devtools/msys2-bootstrap.sh</automated>
    <fails_when>any output line does not end with `eol: lf`</fails_when>
    <automated>git ls-files --eol htslib/configure htslib/Makefile bcftools/vcfsom.c</automated>
    <fails_when>any row shows `w/crlf` or `w/mixed` in the working-tree column</fails_when>
    <automated>sh -n devtools/msys2-bootstrap.sh</automated>
    <fails_when>non-zero exit (shell syntax error)</fails_when>
    <automated>grep -cF "mingw-w64-ucrt-x86_64-python mingw-w64-ucrt-x86_64-python-pip mingw-w64-ucrt-x86_64-python-setuptools mingw-w64-ucrt-x86_64-python-cython mingw-w64-ucrt-x86_64-toolchain mingw-w64-ucrt-x86_64-llvm-tools mingw-w64-ucrt-x86_64-zlib mingw-w64-ucrt-x86_64-bzip2 mingw-w64-ucrt-x86_64-xz" devtools/msys2-bootstrap.sh</automated>
    <fails_when>output is not exactly `1` (PKGS single-line list missing or reformatted)</fails_when>
    <automated>git status --porcelain htslib samtools bcftools</automated>
    <fails_when>any non-empty output (renormalization must not dirty the index)</fails_when>
  </verify>
  <acceptance_criteria>
    - .gitattributes contains all five new eol rules and still contains the original export-ignore and linguist-vendored lines
    - htslib/configure first line ends with LF only (no CR byte before the newline)
    - devtools/msys2-bootstrap.sh exists, passes sh -n, gates on MSYSTEM=UCRT64, carries the exact nine-package single-line PKGS list, exports HTSLIB_CONFIGURE_OPTIONS=--disable-libcurl, creates _venv with --system-site-packages, invokes the canonical editable build command with --no-build-isolation, and ends by running the smoke script
    - devtools/msys2-bootstrap.sh documents the llvm-tools/binutils pacman prompt, the first-run terminate-and-rerun behavior, and the clean-rebuild troubleshooting step
    - Two commits exist: one for .gitattributes, one for the bootstrap script
  </acceptance_criteria>
  <done>Working tree is LF-clean for all bundled sources/scripts; the committed bootstrap script is the provisioning contract whose PKGS list plans 02/03 must mirror verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Create the committed smoke test gate</name>
  <files>devtools/smoke_test.py</files>
  <read_first>
    - .planning/phases/01-mingw-w64/01-RESEARCH.md (section: Code Examples — Smoke test skeleton, verified this session against tests/pysam_data/ composition: ex1.sam.gz and ex1.vcf.gz + .tbi committed; no plain BAM committed)
    - devtools/import.py (standalone devtools-script style analog: argv/sys.exit, no pytest)
    - .claude/CLAUDE.md (anti-pattern table: text-mode I/O is blocking — every direct open call must carry an explicit binary mode)
  </read_first>
  <action>
    Create `devtools/smoke_test.py` per D-04, following the RESEARCH.md smoke skeleton. Requirements:
    - Standalone script style (no pytest): `main()` returning an exit code, `sys.exit(main())` under `__main__`; print `smoke OK` as the final success line.
    - Resolve the repo root from the script location: `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` — test data must be found regardless of the invocation cwd.
    - Step 1 — generate a BAM in a `tempfile.TemporaryDirectory` from `tests/pysam_data/ex1.sam.gz` via `pysam.samtools.view("-b", "-o", bam, sam_path)`. This exercises the samtools dispatch path (the plan 02 devnull fix keeps it working).
    - Step 2 — binary-mode verification of the produced file: open the BAM with mode `"rb"` and assert the first four bytes equal `b"BAM\x01"`.
    - Step 3 — open it via `pysam.AlignmentFile(bam, "rb")` and count `fetch(until_eof=True)`; assert the count is greater than zero.
    - Step 4 — bcftools dispatch: `pysam.bcftools.view("-H", os.path.join(datadir, "ex1.vcf.gz"))`; assert the result is non-None and non-empty. This exercises the catch-stdout plumbing.
    - Every direct `open()` call carries an explicit binary mode (blocking anti-pattern guard from project instructions). No text-mode file I/O anywhere in the script.
    - Any failure (exception or assertion) exits nonzero: wrap main in a try/except that prints the error to stderr and returns 1. Design the checks as discrete, individually named steps so a Phase 4 CI failure log points at the broken capability (D-04 reuse).
    - Commit with message `build(01): add Windows smoke test gate`.
  </action>
  <verify>
    <automated>python -m py_compile devtools/smoke_test.py</automated>
    <fails_when>non-zero exit (script does not parse)</fails_when>
    <automated>python -c "import ast,sys; t=ast.parse(open('devtools/smoke_test.py',encoding='utf-8').read()); calls=[n for n in ast.walk(t) if isinstance(n,ast.Call) and getattr(n.func,'id','')=='open']; bad=[c for c in calls if not (len(c.args)>1 and isinstance(c.args[1],ast.Constant) and 'b' in c.args[1].value)]; print('open calls:',len(calls),'non-binary:',len(bad)); sys.exit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or the printed non-binary count is greater than 0</fails_when>
    <automated>grep -cF 'b"BAM\x01"' devtools/smoke_test.py</automated>
    <fails_when>output is `0` (BAM magic assertion missing)</fails_when>
    <automated>grep -c "bcftools.view" devtools/smoke_test.py</automated>
    <fails_when>output is `0` (bcftools dispatch missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - devtools/smoke_test.py imports pysam, generates a BAM from tests/pysam_data/ex1.sam.gz via pysam.samtools.view, asserts the BAM magic bytes, reads it back via pysam.AlignmentFile with a positive fetch count, and dispatches pysam.bcftools.view on ex1.vcf.gz
    - The AST check proves every direct open call has an explicit binary mode
    - The script prints `smoke OK` only when all steps pass and exits nonzero otherwise
    - The file is committed
  </acceptance_criteria>
  <done>The phase acceptance gate exists as a committed, syntax-valid, binary-safe script ready for plan 03 to execute against a real build.</done>
</task>

<task type="auto">
  <name>Task 3: Document the Windows build path in INSTALL</name>
  <files>INSTALL</files>
  <read_first>
    - INSTALL (existing RST structure: section underlines with `=`, literal blocks with `::`; find where install sections end so the new section follows the same order)
    - devtools/msys2-bootstrap.sh (Task 1 output — the docs must point at it and agree with it, D-13)
    - .planning/phases/01-mingw-w64/01-CONTEXT.md (decisions D-05, D-14, D-15 govern content)
  </read_first>
  <action>
    Add a `Windows (MSYS2 UCRT64)` section to INSTALL, styled like the existing sections (RST underline with `=`, literal blocks with `::`). Content, in order:
    - Step 1 (manual, D-15): install MSYS2 from https://www.msys2.org/ using the official installer, then open the UCRT64 shell (not MINGW64) and let the first `pacman -Syu` finish. One sentence noting msvcrt-flavored MinGW environments are unsupported (python.org CPython fd/heap ABI corruption risk is the project anti-pattern).
    - Step 2 (scripted): from the pysam checkout, run `sh devtools/msys2-bootstrap.sh` in the UCRT64 shell — a literal block. State that the script installs the nine pacman toolchain/library packages, creates `_venv` (D-07), and finishes with the smoke gate.
    - The canonical one-command build (D-14): `pip install -e . --no-build-isolation` inside the activated `_venv`. Do not document any direct `setup.py build` invocation.
    - One sentence per D-05: building with GCC against python.org CPython is explicitly unsupported in v1; end-user Python support is delivered by the upcoming MSVC build path.
    - One sentence: `HTSLIB_MODE` must be unset or `shared` for the Windows build; the script enforces this.
    - One sentence: remote I/O (libcurl/S3/GCS) is disabled on Windows in v1; the script passes --disable-libcurl.
    - Commit with message `docs(01): document MSYS2 UCRT64 build in INSTALL`.
  </action>
  <verify>
    <automated>grep -c "msys2-bootstrap.sh" INSTALL</automated>
    <fails_when>output is `0` (bootstrap script not referenced)</fails_when>
    <automated>grep -c "UCRT64" INSTALL</automated>
    <fails_when>output is `0` (section missing)</fails_when>
    <automated>grep -c -- "--no-build-isolation" INSTALL</automated>
    <fails_when>output is `0` (canonical command not documented)</fails_when>
    <automated>grep -ci "python.org" INSTALL</automated>
    <fails_when>output is `0` (D-05 unsupported sentence missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - INSTALL has a Windows (MSYS2 UCRT64) section documenting exactly two manual steps (MSYS2 install, open UCRT64 shell) and the scripted bootstrap
    - The canonical command `pip install -e . --no-build-isolation` appears; no `setup.py build` invocation is documented as a Windows path
    - The D-05 unsupported sentence, the HTSLIB_MODE requirement, and the --disable-libcurl v1 boundary are present
    - Section styling matches existing INSTALL sections (RST underline, literal blocks)
  </acceptance_criteria>
  <done>The Windows build path is discoverable in-repo, consistent with the committed script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pacman repos -> dev machine | MSYS2 packages (toolchain, python, llvm-tools, libs) cross onto the machine via pacman |
| git working tree -> build inputs | CRLF/encoding corruption of vendored htslib/samtools/bcftools sources crosses into compilation |
| build env vars -> configure invocation | HTSLIB_MODE / HTSLIB_CONFIGURE_OPTIONS flow into the existing build pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | pacman package installs (bootstrap script) | high | mitigate | GPG-signed MSYS2 repos only; no PyPI binary installs on the build path (venv + pacman setuptools/Cython + --no-build-isolation); Package Legitimacy Audit in 01-RESEARCH.md approves all nine packages with zero [ASSUMED]/[SUS] verdicts, so no blocking legitimacy checkpoint is required |
| T-01-02 | Tampering | vendored sources via CRLF checkout corruption | medium | mitigate | .gitattributes eol=lf rules + renormalization in Task 1 restore byte-fidelity; verified by check-attr and ls-files --eol gates |
| T-01-03 | Tampering | toolchain binary substitution on PATH | low | mitigate | bootstrap verifies llvm-nm presence and version after install; UCRT64 MSYSTEM gate anchors the expected environment |
| T-01-04 | Tampering/Elevation | HTSLIB_CONFIGURE_OPTIONS into the pre-existing shell=True configure string | medium | accept | Pre-existing dev-only surface (setup.py:61-73); plan 02 adds no new shell=True call sites; bootstrap sets the var to the constant --disable-libcurl, narrowing what flows through it in the documented flow |
</threat_model>

<verification>
- `git check-attr eol` returns `eol: lf` for all covered bundled paths, and `git ls-files --eol` shows w/lf on the previously mangled files
- `sh -n devtools/msys2-bootstrap.sh` passes; the fixed-string PKGS grep returns exactly 1
- `python -m py_compile devtools/smoke_test.py` passes; the AST binary-mode check reports zero non-binary open calls
- INSTALL greps: msys2-bootstrap.sh, UCRT64, --no-build-isolation, python.org all present
- `git log --oneline -4` shows three commits (attributes, bootstrap, smoke) plus the docs commit
</verification>

<success_criteria>
- The CRLF Wave-0 blocker (Pitfall 1) is closed: any fresh checkout/restore of the bundled trees produces runnable shell scripts and Makefiles
- The provisioning contract (script + docs) is committed and its PKGS list is fixed as the single source of truth for plan 02's fail-fast message
- The acceptance gate (D-04) is committed, binary-safe, and ready to execute against a real UCRT64 build in plan 03
</success_criteria>

<output>
Create `.planning/phases/01-mingw-w64/01-01-SUMMARY.md` when done
</output>
