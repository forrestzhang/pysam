---
phase: 01-mingw-w64
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - setup.py
  - pysam/libcutils.pyx
  - win32/unistd.h
  - win32/stdint.h
autonomous: true
requirements:
  - BUILD-01
  - BUILD-03

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Windows Python outside UCRT64 that imports setup.py exits immediately with one actionable message naming the nine pacman packages and the bootstrap script (D-03)"
    - "On Windows the symbol conflict check runs via llvm-nm and is called outside the exception-swallowing wrapper, so a missing or failing tool aborts the build; POSIX keeps the existing skip-with-warning behavior byte-identically (BUILD-03)"
    - "win32/getopt.c is compiled into pysam.libchtslib only — the other twelve module dicts carry no os_c_files entry — so the now-mandatory symbol check cannot false-positive on getopt_long/opt* duplicates (Pitfall 4)"
    - "Windows link lines carry no ELF rpath flag, and pysam.libchtslib emits an import library named pysam/lib<EXT_SUFFIX-stem>.dll.a that the downstream -l flags resolve through library_dirs (Pitfalls 6/7)"
    - "Every setup.py change is inside a win32-gated block (sys.platform == 'win32' or platform.system() == 'Windows' or os.name == 'nt'); POSIX behavior is untouched (D-08, zero-regression constraint)"
    - "The two hardcoded null-device byte paths in pysam/libcutils.pyx (lines 383, 390) are gone, replaced by os.devnull (Pitfall 8)"
  artifacts:
    - path: "setup.py"
      provides: "UCRT64 fail-fast gate, sh-prefixed configure on win32, llvm-nm symbol-check port, rewritten win32 branch with random/srandom macros, getopt single-copy module lists, libchtslib import-library flag, rpath elif"
      contains: "llvm-nm"
    - path: "pysam/libcutils.pyx"
      provides: "null-device portability in the _pysam_dispatch stdout plumbing"
      contains: "os.devnull"
  key_links:
    - from: "setup.py (fail-fast gate message)"
      to: "devtools/msys2-bootstrap.sh PKGS list"
      via: "the same nine full pacman package names appear verbatim in both (D-03 + D-13 single source of truth)"
      pattern: "mingw-w64-ucrt-x86_64-llvm-tools"
    - from: "setup.py (pysam.libchtslib extra_link_args)"
      to: "the 12 downstream module -l<EXT_SUFFIX-stem> flags"
      via: "-Wl,--out-implib,pysam/lib<stub>.dll.a lands in library_dirs (pysam/), satisfying GNU ld's lib<name>.dll.a search rule"
      pattern: "out-implib"
    - from: "setup.py (_nm_command on win32)"
      to: "check_ext_symbol_conflicts parser"
      via: "llvm-nm -P emits POSIX.2 format, so the existing two-column parser and keep-filter run unchanged (BUILD-03)"
      pattern: "llvm-nm"
  prohibitions:
    - statement: "MUST NOT enable libcurl or any remote I/O on the Windows build path — configure must receive --disable-libcurl on Windows (REMOTE-01 deferred to v2; pre-locked project constraint)"
      status: unverified
      flagged: true
    - statement: "MUST NOT weaken or silently bypass the symbol conflict gate on Windows to make the build pass — no silent skip on win32, and the getopt duplicate false-positive is fixed by single-copy compilation, never by relaxing the keep-filter (symtype not in \"UFNWw\") or deleting legitimate shared symbols"
      status: unverified
      flagged: true

---

<objective>
Re-plumb `setup.py` for MSYS2 UCRT64 with seven surgical, win32-gated edits (D-08 style) so the pipeline that today hard-dies on Windows (cmd.exe configure, missing nm, dead win32 branch, rpath flag, 13x getopt, null-device paths) becomes a working UCRT64 build path — without touching POSIX behavior.

Purpose: BUILD-01 (build pipeline) and BUILD-03 (mandatory symbol gate) are implemented here; plan 03 proves them against a real build.

Output: setup.py carrying all seven edits, libcutils null-device fix, stale shims deleted (D-10), wave-1 head pushed to the fork with ubuntu+macos CI green (D-11 early signal).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mingw-w64/01-CONTEXT.md
@.planning/phases/01-mingw-w64/01-RESEARCH.md
@.planning/phases/01-mingw-w64/01-PATTERNS.md
</context>

## Artifacts this phase produces

- `devtools/msys2-bootstrap.sh` — new committed UCRT64 provisioning script (D-13/D-15) — created by plan 01
- `devtools/smoke_test.py` — new committed acceptance smoke gate (D-04; reused as the Phase 4 CI smoke gate) — created by plan 01
- `devtools/probe_symbol_check.py` — new committed BUILD-03 teeth probe (created by plan 03)
- `.gitattributes` — new eol=lf rules for htslib/**, samtools/**, bcftools/**, *.sh, *.pyx — created by plan 01
- `INSTALL` — new "Windows (MSYS2 UCRT64)" section — created by plan 01
- `setup.py` — new module-level function `_is_ucrt64_python()`; new module-level function `_nm_command()`; the dead win32 branch rewritten in place (D-09); `define_macros` entries `('random', 'rand')` and `('srandom', 'srand')`; `-Wl,--out-implib,pysam/lib<EXT_SUFFIX-stem>.dll.a` link flag on pysam.libchtslib; `sh ` prefix inside `run_configure` on win32; win32 elif in `cy_build_ext.build_extension`; restructured symbol-check dispatch in `cy_build_ext.run`
- `pysam/libcutils.pyx` — two `c_open(force_bytes(os.devnull), O_WRONLY)` call sites (lines 383 and 390)
- Deleted: `win32/unistd.h`, `win32/stdint.h` (D-10)
- Built artifacts (never committed): `htslib/config.h`, `htslib/libhts.a`, `pysam/config.py`, 13 `pysam/libc*.pyd` extension modules, `pysam/libchtslib.*.dll.a` import library

<tasks>

<task type="auto">
  <name>Task 1: Add the UCRT64 fail-fast gate, sh-prefixed configure, and rpath elif</name>
  <files>setup.py</files>
  <read_first>
    - setup.py lines 1-120 (imports, IS_DARWIN, run_configure; place the gate after the log logger definition, before any subprocess can fire) and lines 419-423 (the non-darwin rpath branch inside cy_build_ext.build_extension)
    - .planning/phases/01-mingw-w64/01-PATTERNS.md (Edit 1/2/4 specs and the official MSYS2 detection snippet)
    - devtools/msys2-bootstrap.sh (plan 01 output — the gate message must carry the identical nine-package list, D-03/D-13)
  </read_first>
  <action>
    Three edits in setup.py, per D-03 and D-08 (no restructuring, POSIX paths byte-identical):
    1. Gate (D-03), inserted near the top after the `log` logger definition and before `run_configure`: define `_is_ucrt64_python()` returning True whenever os.name is not nt (POSIX never gated); on nt, True only when `sysconfig.get_platform().startswith("mingw")` AND `os.environ.get("MSYSTEM") == "UCRT64"` (the official MSYS2 detection snippet plus the MSYSTEM belt-and-braces per the research discretion resolution). Immediately after the definition, if not `_is_ucrt64_python()`, call `sys.exit(...)` with a message that: states Windows builds require the MSYS2 UCRT64 environment and its pacman python; points at `sh devtools/msys2-bootstrap.sh`; and lists all nine pacman package names exactly as the bootstrap script's PKGS list spells them (mingw-w64-ucrt-x86_64-python, -python-pip, -python-setuptools, -python-cython, -toolchain, -llvm-tools, -zlib, -bzip2, -xz).
    2. sh prefix (Pitfall 2): in `run_configure` (setup.py:63-68), gate the joined command string on `sys.platform == 'win32'` so Windows prepends `sh ` to `./configure` (shell=True spawns cmd.exe there); the POSIX string stays byte-identical. Do not add any new shell=True call sites.
    3. rpath elif (Pitfall 7): in `cy_build_ext.build_extension` (setup.py:419-423), convert the current non-darwin `else` branch into `elif sys.platform == 'win32':` (which only ensures extra_link_args is a list, adding no flag — PE has no ELF rpath) followed by the existing `else` branch unchanged, so POSIX keeps appending `-Wl,-rpath,$ORIGIN` exactly as before.
    Commit with message `build(01): UCRT64 gate, sh-prefixed configure, win32 rpath branch`.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('setup.py', encoding='utf-8').read())"</automated>
    <fails_when>non-zero exit (setup.py no longer parses)</fails_when>
    <automated>python setup.py --version 2>&1 | grep -c UCRT64</automated>
    <fails_when>output is `0` (the gate message on a non-UCRT64 Windows Python lacks the environment name)</fails_when>
    <automated>python setup.py --version >/dev/null 2>&1; echo "exit=$?"</automated>
    <fails_when>output is `exit=0` (the gate did not fire under the miniconda python.org-style interpreter)</fails_when>
    <automated>grep -c "elif sys.platform == 'win32':" setup.py</automated>
    <fails_when>output is `0` (rpath elif missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - setup.py parses under ast and defines _is_ucrt64_python with the exact two-condition UCRT64 test
    - Running setup.py with the local non-UCRT64 Windows python exits nonzero and its output names the nine pacman packages and the bootstrap script (D-03)
    - The run_configure command string on win32 starts with `sh ` and the POSIX path is unchanged
    - The non-darwin, non-win32 branch still appends -Wl,-rpath,$ORIGIN; the win32 branch adds no linker flag
    - Committed
  </acceptance_criteria>
  <done>setup.py refuses to build outside UCRT64 with an actionable message and fixes the two invocation hazards (cmd.exe configure, rpath flag) without touching POSIX behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Port the symbol gate to llvm-nm, rewrite the win32 branch, and delete the stale shims</name>
  <files>setup.py, win32/unistd.h, win32/stdint.h</files>
  <read_first>
    - setup.py lines 94-112 (run_nm_defined_symbols and its nested cython_internal helper), 320-337 (check_ext_symbol_conflicts), 355-367 (cy_build_ext.run and the exception-swallowing wrapper around the check), 632-655 (the dead win32 branch, EXT_SUFFIX derivation, internal_htslib_libraries), 699-755 (the 13 module dicts)
    - .planning/phases/01-mingw-w64/01-RESEARCH.md (Patterns 3, 4, 5, 6; Pitfalls 3, 4, 5, 6, 12; Assumptions A1, A2, A7)
    - .planning/phases/01-mingw-w64/01-PATTERNS.md (Edit 3/5/6/7 specs)
    - win32/unistd.h and win32/stdint.h (confirm the mapping list being deleted, per D-10 verification duty)
  </read_first>
  <action>
    Four coordinated edits in setup.py plus two deletions, per BUILD-03, D-09, D-10:
    1. Tool swap (BUILD-03): add a module-level helper `_nm_command()` returning the list `["llvm-nm", "-g", "-P"]` when `sys.platform == 'win32'`, else `["nm", "-g", "-P"]`; change `run_nm_defined_symbols` (setup.py:95) to invoke `_nm_command() + [objfile]`. The `-P` POSIX.2 output contract means the existing parser (line 102-110) and keep-filter (line 104) stay untouched.
    2. Fail-hard dispatch (BUILD-03, Pitfall 3): restructure `cy_build_ext.run` (setup.py:360-367). On `sys.platform == 'win32'`: call `self.check_ext_symbol_conflicts()` directly, outside any exception-swallowing try — a missing llvm-nm must abort the build, never warn. Honor the separate-mode design exemption loudly rather than silently: if HTSLIB_MODE equals separate on win32, log a warning that the check is not applicable in that mode and skip it explicitly. On POSIX: keep the existing try/except structure and its two warning log calls exactly as they are today, byte-identical behavior.
    3. Win32 branch rewrite (D-09): replace the body of the `if platform.system() == 'Windows':` block (setup.py:632-636) in place — keep the same insertion point and the same `platform.system()` key; drop the stale `# Windows compatibility - untested` comment; set `include_os = ['win32']` (all modules keep the win32 include dir so getopt.h resolves); set `os_c_files = ['win32/getopt.c']` (consumed only by pysam.libchtslib after edit 4); extend `define_macros` with the two tuples `('random', 'rand')` and `('srandom', 'srand')` with a comment naming bcftools/vcfsom.c lines 362 and 513 as the live consumers after the unistd shim removal (Assumption A1: harmless if UCRT64 headers already declare them; verify at first build in plan 03).
    4. Getopt single-copy (Pitfall 4): the module dicts at setup.py:699-755 currently append `+ os_c_files` to all 13 sources lists. Remove that concatenation from the twelve dicts other than pysam.libchtslib; pysam.libchtslib (the first dict, setup.py:700-704) keeps it. Add a brief comment on the libchtslib dict: the vendored getopt implementation is compiled once here and every other module resolves getopt_long/opt* through its import library, which is what keeps the now-mandatory symbol check free of legitimate duplicates.
    5. Import library (Pitfall 6, research Pattern 4): still inside `cy_build_ext.build_extension`, in the win32 elif from Task 1, add: when `ext.name == 'pysam.libchtslib'`, append `-Wl,--out-implib,<implib>` to ext.extra_link_args where implib is `os.path.join("pysam", "lib" + stub + ".dll.a")` and stub is `os.path.splitext("chtslib" + suffix)[0]` — the identical EXT_SUFFIX-derived derivation used for internal_htslib_libraries at setup.py:651-655 (reuse the module-level `suffix`; never hardcode a cp3xx tag, D-02). The `-l` flags of the other twelve modules then resolve via GNU ld's lib-name.dll.a search rule inside library_dirs (pysam/ is already there).
    6. Shim deletions (D-10): `git rm win32/unistd.h win32/stdint.h`. UCRT64 GCC ships real unistd.h/stdint.h; the stale shim lacks isatty/fileno and risks header shadowing. win32/getopt.c and win32/getopt.h stay (D-10).
    Commit with message `build(01): port symbol gate to llvm-nm, rewrite win32 branch, drop stale shims`.
  </action>
  <verify>
    <automated>grep -c "llvm-nm" setup.py</automated>
    <fails_when>output is `0` (tool swap missing)</fails_when>
    <automated>test ! -f win32/unistd.h && test ! -f win32/stdint.h && test -f win32/getopt.c && test -f win32/getopt.h; echo "shims=$?"</automated>
    <fails_when>output is not `shims=0` (wrong shim set: obsolete pair must be gone, vendored getopt pair must remain)</fails_when>
    <automated>grep -c "win32/getopt.c" setup.py</automated>
    <fails_when>output is not exactly `1` (getopt must be referenced by exactly one module dict; comments must not repeat the literal)</fails_when>
    <automated>grep -c "out-implib" setup.py</automated>
    <fails_when>output is `0` (import library flag missing)</fails_when>
    <automated>grep -cF "('srandom', 'srand')" setup.py</automated>
    <fails_when>output is `0` (vcfsom macro mapping missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - _nm_command exists and returns the llvm-nm triple on win32; run_nm_defined_symbols consumes it; the parser body is unchanged
    - In cy_build_ext.run, the win32 path calls check_ext_symbol_conflicts outside the try/skip (propagating OSError and CalledProcessError), with a loud explicit separate-mode exemption; the POSIX try/skip and its two warning calls are unchanged
    - The win32 branch block sets include_os, os_c_files, and both define_macros tuples, in the same insertion point with the untested comment gone
    - Exactly one module dict (pysam.libchtslib) references the getopt source; twelve dicts lost the concatenation
    - The libchtslib win32 link args include the out-implib flag whose stub is derived from EXT_SUFFIX (no version literal anywhere in the new code)
    - win32/unistd.h and win32/stdint.h are deleted from git; win32/getopt.c/.h remain
    - Committed
  </acceptance_criteria>
  <done>BUILD-03's port exists in source: llvm-nm on Windows, fail-hard dispatch, and the module topology (getopt single-copy + implib) that lets the mandatory check pass legitimately.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the null-device dispatch paths, run the verification battery, push wave 1, and watch fork CI</name>
  <files>pysam/libcutils.pyx</files>
  <read_first>
    - pysam/libcutils.pyx lines 375-395 (the two null-device c_open call sites in the stdout plumbing; force_bytes is already used at line 381, os is imported at line 7, c_open/O_WRONLY come from posix.fcntl at line 22)
    - .planning/phases/01-mingw-w64/01-RESEARCH.md (Pattern 7b and Pitfall 8; the phase boundary note that the full O_BINARY/text-mode audit belongs to Phase 2 — this task substitutes only the null-device path per the CONTEXT phase boundary)
    - setup.py (post-edit state, to re-run the Task 1 battery after all edits landed)
  </read_first>
  <action>
    1. Null-device fix (Pitfall 8): in `pysam/libcutils.pyx`, change the two call sites at lines 383 and 390 from passing the hardcoded null-device byte literal to `c_open` into passing `force_bytes(os.devnull)` (the same helper already applied to stdout_f at line 381; os.devnull is `nul` on Windows, `/dev/null` on POSIX — behavior-preserving on POSIX). Touch nothing else in the file: the posix.fcntl cimports and all other paths are Phase 2 scope per the phase boundary.
    2. Re-run the full battery from Task 1 (ast parse, gate message grep, gate exit code) to confirm the completed edit set behaves.
    3. Zero-regression self-review (D-08/D-11): run `git diff master -- setup.py pysam/libcutils.pyx` and inspect every hunk — each must be inside a win32/Windows/os.name-gated block or a comment; the POSIX branches (darwin handling, run_make, module metadata, the POSIX try/skip) must show no modifications. Note the outcome in the SUMMARY.
    4. Commit the libcutils fix with message `build(01): os.devnull in _pysam_dispatch stdout paths`.
    5. Push wave 1 to the fork for the early zero-regression signal (D-11): `git push origin win`. If the push is rejected as non-fast-forward (plan 01 committed concurrently), `git pull --rebase origin win` then push again.
    6. Watch fork CI on the pushed head (D-12 mechanism, early signal): with `gh`, list the newest run for branch win (`gh run list --branch win --limit 1 --json databaseId,headSha,status`), confirm its headSha is the pushed SHA, then `gh run watch <databaseId> --exit-status`. Evaluate only the ubuntu and macos matrix jobs as blocking (D-12); the FreeBSD/NetBSD VM jobs are advisory. If gh is not authenticated, surface a `checkpoint:human-action` auth gate (run `gh auth login` in a browser-capable shell) and resume — do not skip CI verification. On any ubuntu/macos failure: fix the regression (the diff review in step 3 is the map), commit, re-push, re-watch.
  </action>
  <verify>
    <automated>grep -c "os.devnull" pysam/libcutils.pyx</automated>
    <fails_when>output is not exactly `2` (both call sites must be converted, no more no less)</fails_when>
    <automated>grep -c "/dev/null" pysam/libcutils.pyx</automated>
    <fails_when>output is greater than `0` (a hardcoded POSIX null-device literal remains)</fails_when>
    <automated>python setup.py --version >/dev/null 2>&1; echo "exit=$?"</automated>
    <fails_when>output is `exit=0` (gate stopped firing after the full edit set)</fails_when>
    <automated>gh run view "$(gh run list --branch win --limit 1 --json databaseId --jq '.[0].databaseId')" --json jobs --jq '[.jobs[] | select(.name | test("ubuntu|macos";"i")) | .conclusion] | length, any(. != "success")'</automated>
    <fails_when>the job count is 0, or the any() field prints true (a blocking ubuntu/macos job did not succeed)</fails_when>
  </verify>
  <acceptance_criteria>
    - pysam/libcutils.pyx lines 383 and 390 pass force_bytes(os.devnull) to c_open; no other line of the file changed
    - The full battery passes: setup.py parses, the non-UCRT64 gate fires with the package list, the llvm-nm/tooling greps from Task 2 still hold
    - git diff master shows every setup.py hunk inside a win32-gated region; POSIX-only lines are untouched
    - origin/win contains the wave-1 head; the fork's ubuntu and macos jobs on that SHA are green (FreeBSD/NetBSD ignored per D-12)
    - Committed and pushed
  </acceptance_criteria>
  <done>Wave-1 build-system edits are complete, POSIX CI is green on the fork, and BUILD-01/BUILD-03 source changes are ready for plan 03's live build proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build env vars -> configure invocation | HTSLIB_CONFIGURE_OPTIONS flows into the pre-existing shell=True configure string |
| built .pyd binaries -> symbol gate | llvm-nm reads PE/COFF outputs; the gate decides build failure |
| vendored C sources -> compilation | define_macros and include paths alter what gets compiled on Windows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering/Elevation | shell=True configure string (setup.py:61-73) | medium | accept | Pre-existing dev-only surface; this plan adds zero new shell=True call sites and gates the whole build behind the UCRT64 check; canon injection concern is owned by the project's security review tooling, not re-minted here |
| T-01-06 | Tampering | symbol gate silently skipped on Windows | high | mitigate | BUILD-03 fail-hard dispatch in Task 2: the win32 path calls the check outside the exception-swallowing wrapper, so a missing/broken llvm-nm aborts the build; plan 03 greps the build log to prove no skip warning appears |
| T-01-07 | Tampering | linker input substitution via library_dirs | low | mitigate | The implib lands in the repo-local pysam/ directory that library_dirs already pins; no system-wide or PATH-dependent library search is introduced |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No npm/pip/cargo installs occur in this phase (pacman GPG-signed packages only, --no-build-isolation); Package Legitimacy Audit table present in 01-RESEARCH.md with zero [ASSUMED]/[SUS] verdicts, so no blocking human checkpoint is inserted |
</threat_model>

<verification>
- `python setup.py --version` under the local non-UCRT64 Windows python exits nonzero with the nine-package message (D-03)
- Source greps hold: llvm-nm tool swap, single getopt reference, out-implib flag, both macro tuples, two os.devnull sites, zero hardcoded null-device literals
- `git diff master -- setup.py pysam/libcutils.pyx` shows only win32-gated hunks; win32/unistd.h and win32/stdint.h are deleted
- Fork CI: ubuntu and macos jobs green on the wave-1 head (D-11/D-12 mechanism)
</verification>

<success_criteria>
- All seven surgical edits (D-08/D-09) are in place with POSIX paths byte-identical, verified by diff inspection and fork CI
- BUILD-03 exists in source exactly as specified: llvm-nm on Windows, fail-hard, never silently skipped, with the getopt single-copy topology that keeps it honest
- The Windows build path is internally consistent (implib name matches the -l stubs; gate message matches the bootstrap PKGS list) and ready for plan 03's first build
</success_criteria>

<output>
Create `.planning/phases/01-mingw-w64/01-02-SUMMARY.md` when done
</output>
